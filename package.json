{
  "name": "vant-weapp-demo",
  "version": "1.0.0",
  "description": "Collection of vant-weapp demos.",
  "main": "app.js",
  "scripts": {
    "dev": "set NODE_ENV=development&&gulp",
    "dev:i": "set NODE_ENV=development&&gulp clean && gulp -i",
    "staging": "set NODE_ENV=staging&&gulp clean && gulp build -i",
    "build": "set NODE_ENV=production&&gulp clean && gulp build -i"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vant/weapp": "^1.6.9"
  },
  "devDependencies": {
    "del": "^6.0.0",
    "flyio": "^0.6.14",
    "gulp": "^4.0.2",
    "gulp-cssnano": "^2.1.3",
    "gulp-dependents": "^1.2.5",
    "gulp-eslint": "^6.0.0",
    "gulp-filter": "^7.0.0",
    "gulp-html-minify": "^0.0.14",
    "gulp-if": "^3.0.0",
    "gulp-jsonmin": "^1.2.0",
    "gulp-less": "^5.0.0",
    "gulp-plumber": "^1.2.1",
    "gulp-rename": "^2.0.0"
  }
}